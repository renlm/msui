import React from 'react';
import { VizTooltipItem } from './types';
interface Props {
    item: VizTooltipItem;
    isPinned: boolean;
}
export declare const VizTooltipHeader: ({ item, isPinned }: Props) => React.JSX.Element;
export {};
