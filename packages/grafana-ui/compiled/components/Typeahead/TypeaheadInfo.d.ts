import React from 'react';
import { CompletionItem } from '../../types';
interface Props {
    item: CompletionItem;
    height: number;
}
export declare const TypeaheadInfo: ({ item, height }: Props) => React.JSX.Element;
export {};
