import React from 'react';
import { SliderProps } from './types';
/**
 * @public
 */
export declare const Slider: {
    ({ min, max, onChange, onAfterChange, orientation, reverse, step, value, ariaLabelForHandle, marks, included, }: SliderProps): React.JSX.Element;
    displayName: string;
};
