import React from 'react';
import { TimeZoneInfo } from '@grafana/data';
interface Props {
    info?: TimeZoneInfo;
}
export declare const TimeZoneDescription: ({ info }: Props) => React.JSX.Element | null;
export {};
