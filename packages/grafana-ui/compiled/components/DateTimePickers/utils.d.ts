import { TimeRange, TimeZone } from '@grafana/data';
export declare function isValid(value: string, roundUp?: boolean, timeZone?: TimeZone): boolean;
export declare function isValidTimeRange(range: TimeRange): boolean;
